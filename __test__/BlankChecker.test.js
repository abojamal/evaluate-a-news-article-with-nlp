import { checkForBlank } from '../src/client/js/BlankChecker';

describe('Testing the submit functionality', () => {
  test('Testing the handleSubmit() function', () => {
    1;
    expect(checkForBlank).toBeDefined();
  });
});
